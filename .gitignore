# Secrets — never commit
.env

# Virtual environments
.venv/
.venv313/

# Python caches
__pycache__/
*.pyc

# Run artifacts (regenerated by the pipeline; keep the repo clean for release)
runs/

# OS junk
.DS_Store

# Data / corpus-derived (all-rights-reserved; code-only release)
manifest.csv

# Dev artifacts / not for publication
STATUS.md
.pytest_cache/
.venv313/
*.tar
