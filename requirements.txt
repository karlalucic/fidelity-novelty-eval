--only-binary=:all:
# Core numeric / metrics (pure python axis of the trust table)
numpy
scipy
pandas
scikit-learn
pyyaml
nltk==3.9.2
rank-bm25==0.2.2
krippendorff==0.8.1
# Embedding + authorship proxies (CPU). LUAR loaded via transformers AutoModel(trust_remote_code=True).
torch==2.9.1
transformers==4.57.1
sentence-transformers==5.1.2
# Anthropic generation + LLM-judge (spend-gated; SDK only, key read from env)
anthropic==0.40.0
# NOTE: faststylometry deliberately NOT included (numpy-pin conflict). Burrows's Delta is implemented
# directly in fidelity.py (~40 lines numpy). yt-dlp + ffmpeg are system-wide (used only by fetch.py).
